// lib/forms/finance/theme/finance_theme.dart
//
// Couleur d'accent du module FINANCE PROBAR — un seul fichier par module,
// même convention que hr_theme.dart/recuperable_theme.dart.

import 'package:flutter/material.dart';

const Color kFinanceColor = Color(0xFF0EA5E9);
